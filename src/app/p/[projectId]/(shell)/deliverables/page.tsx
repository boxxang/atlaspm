import { ComingWith } from '@/components/shell/ComingWith';

export default function DeliverablesPage() {
  return (
    <ComingWith title="Deliverables" phase="V2-6">
      Every key deliverable, grouped by stage, and the handover that completes one: a body, an attachment and a completion date.
    </ComingWith>
  );
}
